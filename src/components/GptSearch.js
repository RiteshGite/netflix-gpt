import { BACKGROUND_IMG } from "../utils/constants"

const GptSearch = () => {
  return (
    <div>
      <img src={BACKGROUND_IMG} alt="backgroundImg" className="absolute w-screen h-screen" />
      <form>
        <input/>
        <button></button>
      </form>
    </div>
  )
}

export default GptSearch